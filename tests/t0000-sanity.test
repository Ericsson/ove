#!/usr/bin/env bash

test_sh() {
	local a
	local b

	a=$(readlink -f $(command -v sh))
	b=$(readlink -f $(command -v bash))
	if [ "$a" = "$b" ]; then
		return 0
	fi
	sh -i -c '. ./ove'
	assertEquals "1" "$?"
}

# shellcheck disable=SC1091
source "${OVE_DIR}/tests/common.sh"
